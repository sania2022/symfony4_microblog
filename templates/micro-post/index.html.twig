{% extends 'base.html.twig' %}

{% block body %}

<h6 class="border-bottom border-gray pb-2 mb-0">Recent updates</h6>

        {% for post in posts %}
            <div>
            <p>{{post.text}}</p>
            <span>{{post.time}}</span>
            </div>
        {% endfor %}
{% endblock %}