{% extends 'base.html.twig' %}

{% block body %}


<small class="d-block text-right mt-3">
    <a href="{{path('micro_post_add')}}">Add new</a>
</small>
<h6 class="border-bottom border-gray pb-2 mb-0">Recent updates</h6>
        {% for post in posts %}
            <div>
            <p>{{post.text}}</p>
            <span>{{post.time|date('d/m/Y')}}</span>
            </div>
        {% endfor %}
{% endblock %}